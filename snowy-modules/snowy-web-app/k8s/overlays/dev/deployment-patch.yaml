apiVersion: apps/v1
kind: Deployment
metadata:
  name: snowy-web-app
spec:
  template:
    spec:
      containers:
      - name: web
        env:
        - name: LOG_PATH
          value: "/app/logs"

        # 开发环境不需要特殊覆盖，使用 base 配置即可
