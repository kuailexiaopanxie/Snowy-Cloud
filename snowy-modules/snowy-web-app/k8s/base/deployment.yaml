apiVersion: apps/v1
kind: Deployment
metadata:
  name: snowy-web-app
  labels:
    app: snowy-web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snowy-web-app
  template:
    metadata:
      labels:
        app: snowy-web-app
    spec:
      containers:
      - name: web
        image: snowy-web-app:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 9101
          protocol: TCP
        env:
        # Nacos 服务器地址（来自 ConfigMap）
        - name: SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR
          valueFrom:
            configMapKeyRef:
              name: snowy-web-app-config
              key: nacos.server.addr
        # 环境配置（来自 ConfigMap）
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: snowy-web-app-config
              key: spring.profiles.active
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        volumeMounts:
        - name: logs
          mountPath: /app/logs
        # 健康检查 - 存活探针
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: http
          initialDelaySeconds: 120
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 5
        # 健康检查 - 就绪探针
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: http
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
      volumes:
      - name: logs
        persistentVolumeClaim:
          claimName: snowy-web-app-logs
