apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 测试环境命名空间
namespace: infra

# 引用基础配置
resources:
  - ../../base
  - secret.yaml
  - ingress.yaml

# 策略合并补丁
patches:
  - path: deployment-patch.yaml

# 镜像标签
images:
  - name: snowy-nacos-app
    newTag: latest
